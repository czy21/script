---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/proxy-body-size: '0'
  name: ingress
  namespace: '{{ .Release.Namespace }}'
spec:
  rules:
  - host: jenkins.cluster.com
    http:
      paths:
      - pathType: Prefix
        path: /
        backend:
          service:
            name: jenkins
            port:
              number: 8080
  - host: nexus.cluster.com
    http:
      paths:
      - pathType: Prefix
        path: /
        backend:
          service:
            name: nexus
            port:
              number: 8081
  - host: registry.cluster.com
    http:
      paths:
      - pathType: Prefix
        path: /
        backend:
          service:
            name: nexus
            port:
              number: 5000
  - host: grafana.cluster.com
    http:
      paths:
      - pathType: Prefix
        path: /
        backend:
          service:
            name: grafana
            port:
              number: 3000
  - host: consul.cluster.com
    http:
      paths:
      - pathType: Prefix
        path: /
        backend:
          service:
            name: consul-ui
            port:
              number: 80
  - host: jira.cluster.com
    http:
      paths:
      - pathType: Prefix
        path: /
        backend:
          service:
            name: jira
            port:
              number: 8080
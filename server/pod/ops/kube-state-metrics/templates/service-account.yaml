apiVersion: v1
kind: ServiceAccount
metadata:
  labels:
    app.kubernetes.io/name: {{ .Chart.Name }}
    app.kubernetes.io/version: 2.1.0
  name: {{ .Chart.Name }}
  namespace: {{ .Release.Namespace }}

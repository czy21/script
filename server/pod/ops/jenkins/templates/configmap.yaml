---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Chart.Name }}
  namespace: '{{ .Release.Namespace }}'
  labels:
    app: {{ .Chart.Name }}
data:
  init.gradle: |
    settingsEvaluated { settings ->
        settings.pluginManagement {
            resolutionStrategy {
            }
            repositories {
                gradlePluginPortal()
                maven {
                    name "home"
                    url "http://nexus.cluster2.com/repository/maven-public/"
                    credentials {
                        username "admin"
                        password "Czy20210314."
                    }
                }
            }
        }
    }
    allprojects {
        apply plugin: 'maven-publish'
        publishing {
            repositories {
                maven {
                    name "home"
                    url "http://nexus.cluster2.com/repository/maven-releases/"
                    credentials {
                        username "admin"
                        password "Czy20210314."
                    }
                }
            }
        }
        repositories {
            maven {
                name "home"
                url "http://nexus.cluster2.com/repository/maven-public/"
                credentials {
                    username "admin"
                    password "Czy20210314."
                }
            }
        }
    }
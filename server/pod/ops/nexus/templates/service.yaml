---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Chart.Name }}
  namespace: '{{ .Release.Namespace }}'
spec:
  selector:
    app: {{ .Chart.Name }}
  ports:
  - name: '8081'
    port: 8081
    targetPort: 8081
  - name: '5000'
    port: 5000
    targetPort: 5000
{{- range $i, $e := until (.Values.replicas | int) }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ $.Release.Name }}-{{$i}}
  namespace: '{{ $.Release.Namespace }}'
  labels:
    app: {{ $.Release.Name }}
spec:
  selector:
    statefulset.kubernetes.io/pod-name: {{ $.Release.Name }}-{{$i}}
  type: NodePort
  ports:
  - name: '9092'
    port: 9092
    targetPort: 9092
    nodePort: 909{{add 1 $i}}
{{end}}
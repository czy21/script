[[registry]]
prefix = "docker.io"
location = "{{ param_registry_proxy_url }}"
[[registry]]
prefix = "{{ param_registry_proxy_url }}"
location = "{{ param_registry_proxy_url }}"
[[registry]]
prefix = "{{ param_registry_url }}"
location = "{{ param_registry_url }}"

short-name-mode = "permissive"
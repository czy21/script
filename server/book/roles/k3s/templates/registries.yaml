mirrors:
  docker.io:
    endpoint:
      - "https://{{ param_registry_proxy_url }}"
  {{ param_registry_proxy_url }}:
    endpoint:
      - "https://{{ param_registry_proxy_url }}"
  {{ param_registry_url }}:
    endpoint:
      - "https://{{ param_registry_url }}"
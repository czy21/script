---
apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: demo
  namespace: {{ .Release.Namespace }}
spec:
  hosts:
    - "demo-{{ .Values.param_cluster_name }}.cluster.com"
  gateways:
    - gateway
  http:
  - match:
    - uri:
        prefix: /
    route:
    - destination:
        host: demo-operation-react
        port:
          number: 80
        subset: v1
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ .Release.Name }}
data:
  application-kubernetes.yml: |-
    spring:
      cloud:
        kubernetes:
          configserver:
            config-map-namespaces:
              - {{ .Release.Namespace }}
              - stable
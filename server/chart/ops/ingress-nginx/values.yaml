ingress-nginx:
  controller:
    service:
      type: NodePort
      nodePorts:
        http: 80
        https: 443
    podAnnotations:
      prometheus.io/port: "10254"
      prometheus.io/scrape: "true"
    metrics:
      enabled: true
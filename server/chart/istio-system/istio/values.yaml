istiod:
  pilot:
    env:
      PILOT_HTTP10: true
  # meshConfig:
  #   defaultConfig:
  #     gatewayTopology:
  #       numTrustedProxies: 2
component:
  jaeger: false
  kiali: true

kiali-server:
  auth:
    strategy: anonymous
  deployment:
    image_name: {{ param_registry_proxy if param_proxy and param_registry_proxy else 'quay.io' }}/kiali/kiali
  external_services:
    prometheus:
      url: http://prometheus.ops.svc:9090
    grafana:
      enable: false
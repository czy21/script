apiVersion: security.istio.io/v1
kind: AuthorizationPolicy
metadata:
  name: {{ .Release.Name }}
  namespace: stable
spec:
  selector:
    matchLabels:
      app: sunny-gateway
  action: ALLOW
  rules:
    - to:
        - operation:
            paths: ["*"]
version: '3.7'

services:

  prometheus:
    image: prom/prometheus
    container_name: prometheus
    privileged: true
    ports:
      - 9090:9090
    volumes:
      - ${GLOBAL_CONFIG_DIR}/prometheus/prometheus.yml:/etc/prometheus/prometheus.yml
      - ${GLOBAL_VOLUMES_DIR}/prometheus/:/prometheus/
    restart: always
    user: root

  grafana:
    image: grafana/grafana
    container_name: grafana
    privileged: true
    ports:
      - 3000:3000
    restart: always
    user: root
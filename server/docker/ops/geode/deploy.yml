version: "3.9"

x-traefik-label: &traefik-label
  traefik.enable: true
  traefik.http.routers.{{ param_role_name }}.rule: Host(`geode.cluster.com`)
  traefik.http.routers.{{ param_role_name }}.service: {{ param_role_name }}
  traefik.http.services.{{ param_role_name }}.loadbalancer.server.port: 7070

services:
  geode:
    image: apachegeode/geode:1.15.1
    pull_policy: always
    container_name: geode
    hostname: geode
    labels:
      <<: *traefik-label
    expose:
      - "7070"
    volumes:
      - {{ param_docker_data }}/{{ param_role_name }}/data/:/data/
    command: gfsh start server --name=server1
    restart: always
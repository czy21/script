version: "3.9"

services:

  node-exporter:
    image: prom/node-exporter:v1.11.1
    container_name: node-exporter
    network_mode: host # 9100
    volumes:
      - '/:/host:ro,rslave'
    command:
      - '--path.rootfs=/host'
    restart: always

http:
  routers:
    heimdall:
      rule: Host(`heimdall.czy21.com`)
      service: heimdall@docker
    redis-exporter:
      rule: "Host(`redis-exporter-{{ param_cluster_name }}.czy21.com`)"
      service: redis-exporter@docker
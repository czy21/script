http:
  routers:
    metrics:
      rule: "Host(`traefik-{{ param_cluster_name }}.czy21.com`) && PathPrefix(`/metrics`)"
      service: prometheus@internal
#  middlewares:
#    https-redirect:
#      redirectScheme:
#        scheme: https
#        permanent: true
#tls:
#  certificates:
#    - certFile: /etc/traefik/cert/server.crt
#      keyFile: /etc/traefik/cert/server.key
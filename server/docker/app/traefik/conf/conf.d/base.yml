http:
  routers:
    traefik:
      rule: Host(`traefik-{{ param_node_name }}.czy21.com`)
      service: traefik@docker
    cadvisor:
      rule: Host(`cadvisor-{{ param_node_name }}.czy21.com`)
      service: cadvisor@docker
  middlewares:
    basic-auth:
      basicAuth:
        usersFile: "/etc/traefik/conf.d/basic-auth.txt"
version: '3.9'

services:
  homeassistant:
    image: homeassistant/home-assistant:stable
    container_name: homeassistant
    privileged: true
    user: root
    ports:
      - "8123:8123"
    restart: always
networks:
  default:
    external:
      name: local

version: "3.9"

services:
  ha:
    image: homeassistant/home-assistant:stable
    container_name: ha
    privileged: true
    user: root
    expose:
      - "8123"
    volumes:
      - {{ param_docker_data }}/{{ param_role_name }}/data/:/config/
    restart: always



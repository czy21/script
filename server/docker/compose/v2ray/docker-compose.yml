version: '3.7'

services:

  v2ray:
    image: v2ray/official:latest
    container_name: v2ray
    network_mode: host
    volumes:
      - /data/volumes/v2ray/:/etc/v2ray
    command: v2ray -config=/etc/v2ray/config.json
    restart: always
version: '3.7'

services:

  nexus:
    image: sonatype/nexus3
    container_name: nexus
    ports:
      - 8081:8081
    volumes:
      - /data/volumes/nexus/:/nexus-data/
    user: root
    restart: always



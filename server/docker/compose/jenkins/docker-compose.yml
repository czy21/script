version: '3.7'

services:

  jenkins:
    image: jenkins/jenkins:lts-centos
    container_name: jenkins
    ports:
      - 8090:8080
      - 50000:50000
    volumes:
      - /data/volumes/jenkins/:/var/jenkins_home/
    user: root
    restart: always
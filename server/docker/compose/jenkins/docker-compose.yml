version: '3.7'

services:

  jenkins:
    image: jenkinsci/blueocean
    container_name: jenkins
    privileged: true
    ports:
      - 8090:8080
      - 50000:50000
    volumes:
      - /data/volumes/jenkins/:/var/jenkins_home/
    user: root
    restart: always
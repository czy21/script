version: '3.7'

services:

  shadowsocks:
    image: teddysun/shadowsocks-libev
    container_name: ss-libev
    ports:
      - "40879:9000"
      - "40879:9000/udp"
    volumes:
      - /data/volumes/shadowsocks/:/etc/shadowsocks-libev
    restart: always
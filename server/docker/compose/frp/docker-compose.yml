version: '3.7'

services:

  frp:
    image: frp:1.0.0
    container_name: frp
    privileged: true
    network_mode: host
    volumes:
      - /data/config/frp/conf/:/usr/local/frp/conf/
    restart: always
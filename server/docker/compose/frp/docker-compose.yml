version: '3.7'

services:

  frp:
    image: frp:1.0.0
    container_name: frp
    privileged: true
    ports:
      - "7000:7000"
      - "7500:7500"
    expose:
      - 80
      - 443
    volumes:
      - /data/config/frp/conf/:/usr/local/frp/conf/
    restart: always
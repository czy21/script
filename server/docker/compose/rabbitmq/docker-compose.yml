version: '3.7'

services:

  mq:
    image: rabbitmq:3.8.8-management
    container_name: rabbitmq
    volumes:
      - /data/config/rabbitmq/:/etc/rabbitmq/
      - /data/volumes/rabbitmq/:/var/lib/rabbitmq/
    restart: always
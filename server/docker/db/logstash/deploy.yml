version: "3.9"

services:
  logstash:
    image: logstash:7.17.10
    container_name: logstash
    privileged: true
    user: root
    volumes:
      - {{ param_docker_data }}/app/log/:/app/log/
    restart: always
server {
    listen 80;
    server_name localhost;
    
    location / {
        root /data/firmware/;
        index index.html;
        try_files $uri $uri/ /index.html;
    }

    location /download {
        root /data/;
        autoindex on;
    }

}
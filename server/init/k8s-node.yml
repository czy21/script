- hosts: nodes
  remote_user: root
  vars:
    remote_script: "/{{ansible_user_id}}/{{script_name}}"
    option_name: "k8s"
  tasks:
    - name: copy script
      copy:
        src: "{{item.src}}"
        dest: "{{item.dest}}"
      with_items:
        - { src: "{{script_path}}/{{option_name}}", dest: "{{remote_script}}" }
    - name: install
      shell: bash {{remote_script}}/{{option_name}}/{{option_name}}.sh

    - name: join cluser
      shell: kubeadm join 192.168.2.25:6443 --token 11slnj.i2evoy6005cvhnc0 --discovery-token-ca-cert-hash sha256:c57984ecf8f6c7bde09e6e05fe62544e55dd4cd29b268ca840d76d103358811d
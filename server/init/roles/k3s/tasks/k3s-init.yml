- name: k3s init
  shell: |
    curl -sfL https://get.k3s.io | INSTALL_K3S_VERSION="v{{ param_k3s_version }}" INSTALL_K3S_EXEC="server" K3S_TOKEN="{{ param_k3s_token }}" sh -s - --disable=traefik
  when: inventory_hostname == groups["master"][0]

- import_tasks: install.yml
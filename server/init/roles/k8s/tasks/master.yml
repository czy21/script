- name: kubeadm init
  become: yes
  shell: |
    sudo kubeadm init --config {{ param_remote_role_path }}/k8s-init-config.yml
    sudo -i eval 'if [ -z ${KUBECONFIG} ];then echo 'export KUBECONFIG=/etc/kubernetes/admin.conf' >> ~/.bashrc; fi;'
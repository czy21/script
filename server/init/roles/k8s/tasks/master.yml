- name: kubeadm init
  become: yes
  shell: kubeadm init --config {{ param_remote_role_path }}/config.yml

# TODO unfinished 
# - name: export admin.conf
#   become: yes
#   shell: echo 'export KUBECONFIG=/etc/kubernetes/admin.conf' >> ~/.bashrc
#   when: "{{ lookup('env','KUBECONFIG') == ''}}"
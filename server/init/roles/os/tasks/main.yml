- include_tasks: roles/common/tasks/copy-template.yml

- name: init os
  shell: |-
    timedatectl set-timezone Asia/Shanghai
    bash {{ param_remote_role_path }}/{{ ansible_distribution | lower}}.sh
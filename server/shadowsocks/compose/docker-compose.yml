version: '3.7'

services:

  hadoop:
    image: shadowsocks:1.0.0
    container_name: shadowsocks
    privileged: true
    ports:
      - "4022:22"
      - "40879:40879"
    volumes:
      - /data/volumes/hadoop/sys/fs/cgroup/:/sys/fs/cgroup/
    restart: always
plugins {
    id 'groovy'
    id 'io.spring.dependency-management' version "1.0.11.RELEASE"
}

group 'org.ops'
version '1.0-SNAPSHOT'

repositories {
    maven {
        url "https://repo.jenkins-ci.org/public/"
    }
}

dependencyManagement {
    imports {
        mavenBom "io.jenkins.tools.bom:bom-2.346.x:1451.v15f1fdb_772a_f"
    }
    dependencies {
        dependency "org.apache.groovy:groovy:4.0.3"
    }
}

dependencies {
    implementation 'org.apache.groovy:groovy'
    implementation "org.jenkins-ci.plugins.workflow:workflow-cps"
}

sourceSets {
    main {
        groovy {
            srcDirs = ['src', "vars"]
        }
    }
    test {
        groovy {
            srcDirs = ['test']
        }
    }
}